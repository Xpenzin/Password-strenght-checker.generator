import random

values = []
while True:
    user = input("Choose subjects to randomize(press q when u finish addint to the list): ")
    if user.lower() == "q":
        break
    else:
        print("SUCCESFULLY ADDED")
        values.append(user)       
random.shuffle(values)
print(values[0])